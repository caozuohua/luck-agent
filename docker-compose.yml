services:
  luck-agent:
    build: .
    restart: unless-stopped
    env_file: .env
    volumes:
      - ./data:/opt/luck-agent/data
      - ./soul:/opt/luck-agent/soul
      - ./workspace:/opt/luck-agent/workspace
    ports:
      - "8080:8080"
