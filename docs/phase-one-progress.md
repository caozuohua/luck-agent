# 一期实施进度与证据

目标：[完整一期计划](phase-one-release-plan-2026-09-25.md)。本文件记录阶段事实，不替代计划验收条件。

## 2026-09-25：M0 本地基线（进行中）

- 核对本地分支 main 与远端一致；开始实施计划，保持 Lark API 范围冻结。
- 当前 AWS Tailscale SSH 和已配置公网 SSH 均连接超时。本轮没有获取新的生产差异或改动线上目录；
  9 月 24 日线上 HEAD/脏目录记录只能作为历史线索，不能宣称 M0 生产归档完成。
- 全量测试发现旧 Web shutdown 测试存在后台线程调用真实 `os._exit(0)` 的竞态，会令 pytest 未跑完就成功退出。
  已改为替换该测试服务器的 shutdown 方法并等待完成，不再全局替换进程退出函数。
- 修复既有消息查询调用 SDK 不存在的 `with_sender_name` builder 方法。仅修复现有接口，不增加 API/scope。
- 修复后现有环境完整测试：412 passed，71 subtests passed，2 个 SDK 弃用警告；JUnit 报告保存在忽略的 workspace。
- 测试入口增加唯一 JUnit 完成报告检查：退出码 0 但没有完整报告、无测试或报告含失败都不通过。
- 基线依赖解析发现已安装 Lark SDK 1.7.1 要求 websockets <16，但本地安装为 16.1.1。
  候选锁文件保持其余项目依赖基线，解析为兼容 websockets 15.0.1，并记录包哈希。
- 干净 Python 3.12.13 venv 从哈希锁文件安装：53 个包依赖检查通过，完整测试 412 passed / 71 subtests passed。
  Linux x86_64 Python 3.12 依赖解析通过，尚未替代 Linux 运行验收。
- 修复 Docker 构建上下文误排除 `runtime/`，镜像改用哈希锁文件；补排除本地 venv、数据库和备份。
  新增打包回归检查通过；尚未实际构建/启动 Docker 镜像。

## 尚未完成的发布条件

M0：线上私有备份、逐文件差异审查、候选代码归档、Linux 依赖和迁移/回退验证。
结构整理：生产装配隔离、旧路径引用清单与职责边界。
M1–M4：调用契约与事件、结果验收/恢复策略、持久通知、记忆隔离等仍待实施。
M5：固定任务集、故障注入、真实候选验收、48 小时灰度和回滚演练仍待完成。
