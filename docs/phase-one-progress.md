# 一期实施进度与证据

目标：[完整一期计划](phase-one-release-plan-2026-09-25.md)。本文件记录阶段事实，不替代计划验收条件。

## 2026-09-25：M0 本地基线（进行中）

- 核对本地分支 main 与远端一致；开始实施计划，保持 Lark API 范围冻结。
- 当前 AWS Tailscale SSH 和已配置公网 SSH 均连接超时。本轮没有获取新的生产差异或改动线上目录；
  9 月 24 日线上 HEAD/脏目录记录只能作为历史线索，不能宣称 M0 生产归档完成。
- 全量测试发现旧 Web shutdown 测试存在后台线程调用真实 `os._exit(0)` 的竞态，会令 pytest 未跑完就成功退出。
  已改为替换该测试服务器的 shutdown 方法并等待完成，不再全局替换进程退出函数。
- 修复既有消息查询调用 SDK 不存在的 `with_sender_name` builder 方法。仅修复现有接口，不增加 API/scope。
- 修复后现有环境完整测试：412 passed，71 subtests passed，2 个 SDK 弃用警告；JUnit 报告保存在忽略的 workspace。
- 测试入口增加唯一 JUnit 完成报告检查：退出码 0 但没有完整报告、无测试或报告含失败都不通过。
- 基线依赖解析发现已安装 Lark SDK 1.7.1 要求 websockets <16，但本地安装为 16.1.1。
  候选锁文件保持其余项目依赖基线，解析为兼容 websockets 15.0.1，并记录包哈希。
- 干净 Python 3.12.13 venv 从哈希锁文件安装：53 个包依赖检查通过，完整测试 412 passed / 71 subtests passed。
  Linux x86_64 Python 3.12 依赖解析通过，尚未替代 Linux 运行验收。
- 修复 Docker 构建上下文误排除 `runtime/`，镜像改用哈希锁文件；补排除本地 venv、数据库和备份。
  新增打包回归检查通过；尚未实际构建/启动 Docker 镜像。

## 尚未完成的发布条件

M0：线上私有备份、逐文件差异审查、候选代码归档、Linux 依赖和迁移/回退验证。
结构整理：生产装配隔离、旧路径引用清单与职责边界。
M1–M4：调用契约与事件、结果验收/恢复策略、持久通知、记忆隔离等仍待实施。
M5：固定任务集、故障注入、真实候选验收、48 小时灰度和回滚演练仍待完成。

## 2026-09-25：M1 参数执行边界（部分完成）

- 工具注册时检查 schema 有效性，执行前统一验证参数对象、JSON 合法性及工具 schema。
- 内置 shell/search 参数补齐非空、范围和额外字段约束；不存在的 target 参数不再被工具静默忽略。
- 非法参数在权限/审批消费和工具调用前返回结构化 `INVALID_TOOL_ARGUMENTS`，只给出验证规则，不回显拒绝值。
- 命令别名保留显式非法值交给校验，不通过字符串强转/真假值回退把坏参数变成另一个有效命令。
- 新增 26 项执行边界测试全部通过；最终完整回归 439 passed / 71 subtests passed，2 个 SDK 弃用警告。
- 阶段基线已推送 `6aedc6f`；本阶段仍未部署。M1 的持久事件、操作关联、能力可用性和快捷命令统一审计未完成。

## 2026-09-26：M1 操作账本与重放阻断（部分完成）

- 新增 SQLite operation_attempts，工具执行前提交 prepared/executing 记录，终态更新检查实际影响行数。
  记录关联 ID、目标、参数/schema 指纹与部署版本，不保存原始命令、审批 token 或工具输出。
- 生产装配为 ToolExecutor 和快捷 restart/backup 注入同一 OperationStore；图调用具备 goal/run/step/chat
  上下文，快捷调用具备 request/run/chat 上下文。并发上下文互不污染。
- 账本不可用时拒绝发起调用；执行后记录失败不返回成功。超时、异常、取消保留 unknown 或 executing，
  工具返回 ok 仅记 returned_ok，不等于业务验收通过。
- 图入口遇到该 Goal 已启动但尚未对账的写操作时停止重放，包括工具已返回、任务终态尚未提交的崩溃窗口。
  监督节点保留阻断标记，未知结果不能被模型 DONE 或普通审批覆盖。
- 快捷写操作不再在 TypeError 后换签名再次执行；在执行前判定签名。SSH 写操作超时和非零退出码显式标记
  副作用不确定，快捷入口持久化为 unknown 并提示人工核对。
- 本阶段仍未部署。恢复目前停止并报失败；M2 的等待人工/未确认业务状态、结果对账及解除阻断接口尚未接入。
  快捷只读/记忆调用、审批 ID、跨尝试逻辑操作关联、能力预检及完整决策事件仍待后续补齐，不能宣称 M1 完成。
- 验证：新增 19 项操作账本/故障用例；干净锁定环境全量 458 passed / 71 subtests passed，
  2 条既有 SDK 弃用警告。JUnit 完成报告为本地忽略目录
  `workspace/test-results/pytest-e5cf6d6d31854ed6996f4b65fccba18d.xml`。

## 2026-09-26：M1 本地能力预检与请求关联（部分完成）

- 已推送前一阶段操作账本 `221b0c4`。本阶段为工具增加本地配置预检和实时注册表快照；
  缺 SERPER_API_KEY 在调用网络及消费审批之前返回 configuration 阻断，不靠模型反复尝试发现配置缺失。
  预检自身异常也阻断，诊断不回显异常中的配置值。
- 工具提示包含当前本地可用性；明确的本机工具清单问法直接读取注册表，包括历史审查中的错选 web_search 样本。
  仅匹配独立清单问题，复合操作/外部搜索仍走正常执行链。回答明确不等价于授权或远端健康，
  当前也不覆盖快捷运维/技能/MCP 的可用性，不能把这一小范围路由测试当作完整模型选择验收。
- 消息来源转换为稳定哈希 request_id；Goal plan 持久化该 ID，重启恢复后延续关联。
  快捷命令从消息入口继承同一引用。卡片确认消息可能含 token，不将原文作为新请求 ID 保存。
  兼容读取旧 source_message_id 时也先哈希；本次没有批量清洗历史数据库。
- 全量回归 472 passed / 71 subtests passed，2 条既有 SDK 弃用警告；比前一阶段新增 14 项用例。
  完成报告：`workspace/test-results/pytest-1cf99e8648ff4fe1bac13e9d8a2eabaf.xml`。
- 尚未部署；M1 的完整能力清单、审批引用、快捷只读/记忆审计及决策事件仍未完成。

## 2026-09-26：M1 持久决策与审查统计（部分完成）

- 新增 operation_decisions，在生产图监督节点返回下一步之前提交决策及原因码；关联
  goal/run/request/step/operation/attempt，保留策略与部署版本。重启写入阻断及本地能力清单也写入记录。
- 只持久化允许的结构化字段，不复制模型 plan、scratchpad、用户原文、工具输出或原始异常。
  记录失败时终止本轮图执行并给出明确提示；真实 LangGraph 故障注入验证下一次工具调用不会发生。
- 历史审查器读取新操作/决策表，报告覆盖量、状态、原因码和结果落盘至决策落盘的延迟。
  旧库没有新表仍可审查；新旧来源独立展示，不将 checkpoint 副本加入直接调用分母。
  returned_ok 和模型 DONE 仍不当作独立业务验证成功。
- 完整回归 480 passed / 71 subtests passed，2 条既有 SDK 弃用警告；新增 8 项用例。
  报告：`workspace/test-results/pytest-271dffe54c8b433f9933cc80bce0eaaf.xml`。
- 尚未部署。此阶段记录现行决策，没有修正全部决策语义：每操作重试计数/预算、独立业务验收与人工状态仍属 M2。
  生产 HITL=false 路径已覆盖；legacy HITL 暂停前事件、完整生命周期事件及快捷只读/记忆路径尚待补齐。
