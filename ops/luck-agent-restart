#!/bin/sh
set -eu

[ "$#" -eq 0 ] || exit 2
unit="luck-agent-restart-$$"
/bin/systemd-run \
    --quiet \
    --unit="$unit" \
    --collect \
    --on-active=3s \
    --no-block \
    /bin/systemctl restart luck-agent
echo "restart-scheduled"
