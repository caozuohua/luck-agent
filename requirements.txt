# requirements.txt
# ─── V2 Goal Runtime (OpenAI-compatible LLM, no Google deps) ────────
lark-oapi>=1.3.3
httpx>=0.27.0           # Lark file API / OpenAI-compatible LLM client (async)
aiosqlite>=0.20.0      # GoalStore / patterns / context summaries async SQLite
jsonschema>=4.23.0      # Goal / Step / ToolResult / Verification JSON validation

# ─── Graph engine (LangGraph, HARD dependency) ──────────────
# LangGraph 1.2.x splits checkpointer backends into separate
# packages; the SQLite saver lives in langgraph-checkpoint-sqlite.
langgraph>=0.2
langgraph-checkpoint-sqlite>=3.0

# ─── Test only ──────────────────────────────────────────────────────
pytest>=9.0.0
pytest-asyncio>=1.3.0
pytest-cov>=7.0.0

# ─── 标准库替代（已移除的依赖）────────────────────────────────
# google-genai / google-auth  → V1 (agent.py) 已退役，V2 用 OpenAI 兼容客户端
# structlog        → core/log.py（标准库 logging + JSON formatter）
# cachetools       → 未使用（TTLCache 已用 threading.local 替代）
# google-cloud-secret-manager → 已改用 .env 文件

# ─── 以下均为标准库，无需安装 ─────────────────────────────────
# asyncio, sqlite3, pathlib, json, re, signal, logging, threading
