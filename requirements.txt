# requirements.txt
# ─── GCP / Gemini AI (google-genai) ────────────────────────────
# 升级为官方推荐的 google-genai 库
# google-genai 是独立的轻量库，比 google-cloud-aiplatform 更简洁
google-genai>=2.0.0
google-auth>=2.28.0

# ─── Lark ─────────────────────────────────────────────────────
lark-oapi>=1.3.3

# ─── HTTP ─────────────────────────────────────────────────────
httpx>=0.27.0           # GitHub API / Lark 文件 API（异步）

# ─── V2 structured protocol validation ─────────────────────────
jsonschema>=4.23.0      # Goal / Step / ToolResult / Verification JSON 校验

# ─── 标准库替代（已移除的依赖）────────────────────────────────
# structlog        → core/log.py（标准库 logging + JSON formatter）
# cachetools       → 未使用（TTLCache 已用 threading.local 替代）
# google-cloud-secret-manager → 已改用 .env 文件

# ─── 以下均为标准库，无需安装 ─────────────────────────────────
# asyncio, sqlite3, pathlib, json, re, signal, logging, threading
