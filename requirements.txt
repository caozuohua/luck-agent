# requirements.txt
# ─── GCP / Vertex AI ──────────────────────────────────────────
# 去掉 [adk,agent_engines] extras：减少 langchain/cloudpickle 等重型依赖
# 只需核心包即可使用 vertexai.generative_models（Gemini API）
google-cloud-aiplatform>=1.87.0
google-auth>=2.28.0

# ─── Lark ─────────────────────────────────────────────────────
lark-oapi>=1.3.3

# ─── HTTP ─────────────────────────────────────────────────────
httpx>=0.27.0           # GitHub API / Lark 文件 API（异步）

# ─── 标准库替代（已移除的依赖）────────────────────────────────
# structlog        → core/log.py（标准库 logging + JSON formatter）
# cachetools       → 未使用（TTLCache 已用 threading.local 替代）
# google-cloud-secret-manager → 已改用 .env 文件

# ─── 以下均为标准库，无需安装 ─────────────────────────────────
# asyncio, sqlite3, pathlib, json, re, signal, logging, threading
